---
- hosts: metmini-prd
  vars:
    sql_root_password: 'secretsql'

  tasks:

    - name: Retrieve records from test database
      command: mysql -u root -psecretsql --database metminidb --execute "select * from actual"

