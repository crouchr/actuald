---
- hosts: metmini-prd
  vars:
    sql_root_password: 'secretsql'

  tasks:

    - name: Retrieve records from test database
      command: mysql -u root -p secretsql database metminidb select * from actual

